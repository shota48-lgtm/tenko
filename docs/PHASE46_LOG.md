# PHASE46_LOG

tenko Phase 4.6（UIの作り直しと機能1の完成）の作業ログ。

## ブラウザ操作が使えたか

**使えました。** Claude in Chrome の拡張が接続でき、`http://localhost:3000/village` を開いて
スクリーンショットを撮り、クリックと入力もできました。

そのため今回は「画面を見る → 直す → また見る」の繰り返しで進めています。
Phase 4.5 まで一度も見られていなかったヘッダのHTML部分を、初めて実際に見て直しました。

**最初に見た実機の状態**（作り直す前）:
- ヘッダの5行がすべて素のテキストで、ボタンがボタンに見えない
  （原因は Tailwind のリセット。`button` の枠と背景が消えるため、押せるものに見えなくなっていた）
- 「今日やること」の入力欄も枠がなく、どこに入力するのか分からない
- 選択中の状態（在席/離席）が見た目で判別できない
- チャット画面は背景が真っ黒（端末のダークテーマ設定を拾っていた）で、
  枠のない入力欄と送信ボタンが背景に沈んで見えなかった

---

## 記録
### 作業1: 吹き出しの案を確定（完了）

- 案A（Canvasに描く）を削除。`drawBubbles` を消し、頭上の「メモあり」の印だけを描く `drawNoteMarks` に置き換えた
- 切り替えのUIを画面から外した。案Aへ到達する経路は残っていない
- 見た目を村に寄せた: 角丸なし（`rounded` を外し1pxの直角）、背景 `#f6f1e3`（クリーム寄り）、
  枠線は `stone-900` の1px、文字は11px、影を1pxだけ付けてドット絵に馴染ませた
- **位置を直した。** 人物の頭の真上に中心を合わせる。
  配置計算の「見込み幅」ではなく実際の枠幅で中心を取りたいので `translate(-50%, -100%)` を使い、
  人物と吹き出しの距離を3ドットから1ドットに詰めた
- **画面の端の扱いを決めた。** 端では中心合わせをやめ、枠を村の内側に寄せる（左端なら左揃え、右端なら右揃え）。
  実機で左端の吹き出しが切れていたのを見て直した

### 作業2: 機能1の主張を成立させる（完了）

村の右に全員分の一覧を置いた。村は3件のまま。

- 各行は「名前 / 状態 / 今日やること」。未記入の人も「未記入」と出す
- 並びは 会話中 → 在席 → 休憩中 → 離席 → 村にいない
- 村と同時に見える（別ページに飛ばさない）
- 長い本文は2行で切る（`line-clamp-2`）。全文は `title` 属性で読める
- 利用者の一覧は `GET /api/users`（表示名のみ。役割・上長・勤怠は返さない）

**20人で実測**（`scripts/seed-crowd.js`）: 一覧26人がスクロールで収まり、村は吹き出し3件のまま崩れなかった。

### 作業3: ヘッダの作り直し（完了）

作り直す前は、5行すべてが素のテキストだった。原因は Tailwind のリセットで、`button` と `input` の
枠・背景が消えていたこと。**画面を見るまで気づけなかった。**

- ボタンに枠と背景を付けた。選択中は黒地に白抜き（`aria-pressed` も付けた）
- 「今日やること」の入力欄に枠と背景と説明ラベルを付け、文字数（46/80）を横に出した
- ラベル（読むもの）は小さい灰色、操作（押すもの）は枠付きに分けた
- 接続状態は点付きのバッジにした（接続中は緑、切断は琥珀）
- **開発用（見た目のA/B/C、部屋数、村の人数）は `?debug=1` のときだけ出す。** 実機で確認済み
- 行数は5行から2行に減った（開発用を除く）

### 作業4: 同じ利用者の複数接続（完了）

**採用する接続の規則: 最後に状態を申告した接続。**
理由は、手元で操作している端末が最後に申告するため。放置された古い端末の「在席」で上書きされない。

- `ws-server` の `presenceList()` で利用者IDごとに畳む。接続数も併せて配る
- 片方を閉じても、もう片方が生きていれば村から消えない（畳んだ結果に残る）
- 接続は拒否しない
- クライアント側でも畳む。古いサーバーに繋いだ場合に同じ利用者が2人描かれ、
  React の key が重複してエラーになる（実機のコンソールで発見）

**実測**: 2つのタブから同じ利用者で接続 → 村の人物は1体、吹き出しも1つ。

---

## 審査役ごとの指摘と対応

### 審査役A: セキュリティ（UI変更後に全攻撃を再実行）

| 送ったもの | 結果 |
|---|---|
| `<script>alert(1)</script>` / `<img onerror>` | 200。文字列として保存され、表示側でエスケープ |
| 改行・タブ・NUL | 空白に寄せて保存 |
| RLO・ゼロ幅 | 除去 |
| 空文字・空白のみ | 400 |
| 81文字・10万文字 | 400 |
| 絵文字（家族の合字） | そのまま保存（壊れない） |
| `'); DROP TABLE daily_notes; --` | 200で保存。テーブルは健在 |
| 存在しない利用者・負のID | 401 |
| 異常の見える範囲 | 部下=[2] / 無関係=[4] / 別上長=[5] / 担当上長=[2,3] / 管理者=全員。**他人の分は出ない** |
| 無関係な member が target 指定 | 403 |
| 修正申請（理由なし・kind不正・日時不正・201文字・他人の記録） | 400 / 400 / 400 / 400 / 403 |
| 修正申請の承認（4通り） | 本人403 / 無関係403 / 別上長403 / 担当上長200 |

**UIの変更で権限の判定が画面側に移っていないことを確認した。** 判定は `src/lib/actor.ts` と
各APIの中だけにあり、画面はボタンを出し分けていない（承認画面は一覧が空になるだけで、APIが拒否する）。

### 審査役B: 利用者の目（ブラウザで実際に見た）

| 周回 | 何を見て、何が出て、何を直したか |
|---|---|
| 1 | 作り直し前のヘッダを実機で確認。5行すべて素のテキスト、入力欄が見えない、選択中が分からない → 作業3で作り直し |
| 2 | 吹き出しが人物の左上にずれていた → 中心合わせに変更 |
| 2 | 同じ利用者が2人現れた → ws-server とクライアントの両方で畳む |
| 2 | コンソールに `two children with the same key` → 上と同じ原因。畳んで解消 |
| 3 | 20人を投入。村の左端で吹き出しが切れた → 端で内側に寄せる処理を追加 |
| 3 | 一覧の行の高さがばらついた → 2行で切る |
| 4 | **ボタンを押したら画面が固まった。** 在席が変わるたびに村全体（約27万回の描画）を描き直していた → 地面を1回だけ描いて使い回す形に変更。反応が戻った |
| 4 | 46文字の「今日やること」を入力 → 一覧で2行に省略され、崩れない。文字数表示も 46/80 に追従 |
| 4 | 「離席」を押す → 黒地に白抜きへ反転し、フォーカスリングも出る |
| 5 | `?debug=1` を付けたときだけ開発用の行が出ることを確認。付けない場合は出ない |
| 5 | チャット画面が真っ黒だった（端末のダークテーマ）。入力欄と送信ボタンが背景に沈む → 明るい配色に固定し、全画面を同じ基準で整えた |

### 審査役C: 設計の一貫性

- 既存方針（投稿はHTTP+DB / WSは通知路 / 機械は下書きまで）は崩していない。攻撃の再実行で確認
- **案Aの残骸を削除した。** `drawBubbles` を消し、`bubbleMode` の状態と切り替えUIも消した
- 同じことを2箇所に書いていないか: 利用者IDの解決は `actor.ts` の1箇所、
  吹き出しの配置計算は `bubbles.ts` の1箇所。画面側は座標を使うだけ
- 複数接続の畳み込みだけはサーバーとクライアントの2箇所にある。
  **これは重複ではなく二重の防御**（古いサーバーに繋いだ場合に画面が壊れないため）。理由をコードに書いた

### 審査役D: 運用する立場

- 20人で一覧・村ともに破綻しない（実測）。50人は未測定
- 描画の重さは地面の使い回しで解消。人物だけなら人数に比例するが、20人で反応が戻っている
- 一覧は状態順に並ぶため、増えても「会話中の人」が上に来る
- 未記入の人が見えるので、朝の共有が漏れている人が分かる

### 追加の周回（6〜8周目）

| 周回 | 何を見て、何が出て、何を直したか |
|---|---|
| 6 | 承認画面で日付が `2026-08-10T15:00:00.000Z` と生の形式で出ていた → `::text` で日付だけにした |
| 6 | 修正申請の行に「根拠の発言: (下書きなし)」「一致した箇所: - / ルール: -」と、意味のない表示が出ていた → 修正申請には理由と元の記録を見せる形に変えた |
| 6 | 直したのに画面が変わらなかった。PowerShell の文字列置換が当たっておらず、ファイルが書き換わっていなかった。**API の応答を直接見て、サーバー側は正しいと切り分けてから気づいた**（J221） |
| 7 | 一覧の名前が presence（本人の自己申告）由来だった。認証が無い今、URLパラメータで他人の名前を騙れる → **DBの表示名を正に変更**（セキュリティ上の修正） |
| 7 | 一覧に検証の痕跡（「他人になりすまして書き込んだ」）が2件残っていた → `scripts/cleanup-notes.js`（`--apply` を付けない限り消さない）で整理 |
| 8 | 破壊試験を再実行。WS偽投稿・承認済みの変更・確定済み下書きの変更・他人の承認（4通り）すべて意図どおり |

---

# 朝の報告書

## 1. ブラウザ操作が使えたか

**使えました。** そのため今回は実機を見ながら直しています。
Phase 4.5 までPOにしか見えていなかったヘッダのHTML部分を、初めてCCが見て直しました。

見て初めて分かったこと（コードを読んでも気づけなかったもの）:

- ボタンが素のテキストに見えていた原因は Tailwind のリセット（`button` の枠と背景が消える）
- チャット画面が真っ黒だった原因は `prefers-color-scheme: dark`（端末の設定を拾っていた）
- 吹き出しが人物の左上にずれていた
- **ボタンを押すと画面が固まった**（在席が変わるたびに村全体を1ドットずつ約27万回描き直していた）
- 一覧の名前が本人の自己申告（URLパラメータ）由来で、他人の名前を騙れた

## 2. 完了した作業

| 作業 | 状態 |
|---|---|
| 1 吹き出しの案を確定（案B採用・案A削除・位置と見た目） | 完了 |
| 2 機能1の主張を成立させる（全員分の一覧） | 完了 |
| 3 ヘッダの作り直し | 完了 |
| 4 同じ利用者の複数接続 | 完了 |
| 5 審査役による多角レビュー | 9周 |
| 追加: チャット・承認・勤怠の各画面 | 完了 |
| 追加: 描画性能の修正 | 完了 |
| 追加: 深夜0時をまたぐ勤務の未対応を README と STATE に明記 | 完了 |

## 3〜5

「審査役ごとの指摘と対応」「セキュリティ検証」「破壊試験」の各節に記載。

## 6. 何周したか

**9周。** 各周で直したものは上の表のとおり。
指摘がゼロで終わった周は8周目（破壊試験のみ）と9周目（最終確認）。

## 7. 迷った点と、選んだもの・却下したもの

| 迷った点 | 選んだもの | 却下したもの | 理由 |
|---|---|---|---|
| 吹き出しの中心合わせ | `translate(-50%)` で実際の枠幅から中心を取る | 配置計算の見込み幅で `left` を決める | 文字数で枠幅が変わるため、見込み幅では必ずずれる。案Aが不採用になった理由と同じ構造 |
| 画面端での吹き出し | 端では中心合わせをやめ、内側に寄せる | はみ出したまま / 折り返す | 切れて読めないより、位置が少しずれても読める方がよい |
| ダークテーマ | 明るい配色に固定 | 端末の設定に従う | 村の絵が明るい前提の配色。暗い背景では浮く。利用者ごとに見え方が変わると「同じ村を見ている」前提が崩れる |
| 複数接続でどれを採用するか | 最後に申告した接続 | 最初の接続 / 最も「活動的」な状態 | 手元で操作している端末が最後に申告する。放置された端末の在席で上書きされない |
| 複数接続の畳み込みの場所 | サーバーとクライアントの両方 | サーバーだけ | 古いサーバーに繋いだときに画面が壊れる（key の重複）。二重の防御として置き、理由をコードに書いた |
| 一覧の名前 | DBの表示名 | presence の名前 | 認証が無い今、presence の名前は自己申告で他人を騙れる |
| 村が画面に収まらない | 村の枠の中でスクロール | ページ全体をスクロール / 縮尺を自動調整 | ヘッダと一覧を常に見える位置に留めたい。縮尺の自動調整は非整数倍でドット絵がぼける |

## 8. POに判断してほしい点

1. **村の縮尺（2倍固定）。** 画面が縦に狭いと村が枠内スクロールになる。1倍に落とす選択肢もあるが人物が16pxになる
2. **一覧の幅（288px）と位置（右）。** 下に置く案もある
3. **一覧の並び順**（会話中 → 在席 → 休憩中 → 離席 → 村にいない）
4. **吹き出しの上限3件**を維持した点。一覧があるので村は3件でよいと判断した
5. **ダークテーマを用意しない**と決めた点
6. **`?debug=1` に隠した項目**（見た目のA/B/C、部屋数、村の人数）。常時見たいものがあれば戻す
7. 検証用の利用者が26人分DBに残っている（群衆01〜20 など）。消すか残すか

## 9. 現時点で答えを持たない事項

**要調査**
- 実際の職場の人数。20人では成立したが、50人での一覧と村は測っていない
- 一覧が長くなったときの探しやすさ（絞り込みや検索が要るか）
- 自動離席10分の妥当性（実運用の裏付けなし。仮説のまま）

**未検証**
- 自動離席が実際に10分後に発火するか（15秒間隔の判定ロジックは動いているが、10分待つ実測はしていない）
- 複数の端末（PCとスマホ）での実機確認。同一利用者の複数接続はタブ2つでのみ確認
- 画面幅が狭い端末での表示（ヘッダは折り返す作りだが、実機で見ていない）
- WSサーバーを再起動したときの `talk` の復元
- 50人規模での描画の重さ（地面の使い回しで軽くなったが、人物は人数に比例する）

**仮説**
- 「村と一覧の2つで、全員が今日何をしているか分かる」— 形は整ったが、実際に毎朝使われるかは未検証
- 「話しかけてOK/NG が Slack の解けていない問題を解く」— 既定値のまま放置される可能性がある

## 10. 見た目で確認できていない箇所

- **画面幅が狭い場合**（スマホ・タブレット）。ヘッダは `flex-wrap` で折り返すが実機で見ていない
- **自動離席の案内文**（「10分操作がないため離席にしました」）。10分待っていないため一度も表示させていない
- **差し戻しの操作後の画面**（承認画面で差し戻しボタンを押した後の状態）
- **エラー表示**（「本文は80文字までです」などが画面に出た状態）。APIの応答は確認したが画面では見ていない
- **50人以上の一覧**
- **チャット画面のスクロール**（発言が画面を超える量になった状態）

## 11. git log

```n60b2714 fix(village): 一覧の名前をDBの表示名にする
dc57cae fix(approvals): 承認画面の日付と修正申請の見せ方を直す
ea9deb7 docs: 深夜0時をまたぐ勤務が未対応であることを README と STATE に明記
59ca10d perf(village): 地面を一度だけ描いて使い回す
d48e265 feat(ui): 画面を実機で見て作り直し、全員分の一覧を追加
036ba19 docs: 新機能の提案と STATE の更新
3d3a872 refactor(auth): 利用者IDの解決を actor.ts に統一し、朝の報告書を追記
5564493 feat(attendance): 勤怠異常の提示と修正申請を追加
42f28fd fix(bubbles): 再代入しない変数を const にする（lint）
4650572 feat(office): 今日やること・話しかけてOK/NG・自動離席を追加
```n

### 追加の周回（10〜11周目）

| 周回 | 何を試して、何が出て、何を直したか |
|---|---|
| 10 | 80文字を超える入力を画面から送った → 文字数が `85/80` と赤字になり、「本文は 80 文字までです（85 文字）」がヘッダに赤字で出る。**エラー表示を実機で初めて確認した** |
| 10 | 承認画面で差し戻しを実行 → DBは `status=returned`・理由も保存されたが、押した直後の画面が変わらなかった。**押せていないように見える** → 処理中はボタンを無効化し「処理中…」に変える |
| 11 | 画面幅を変えてヘッダの折り返しを実測（ウィンドウのリサイズが効かなかったため、ルート要素の幅を変えて測った） |

**幅ごとのヘッダの高さ（実測）**

| 幅 | ヘッダの高さ | 横スクロール |
|---|---|---|
| 1920px | 86px（2行） | あり（村が2倍で1280px + 一覧288px のため） |
| 1200px | 86px（2行） | なし |
| 900px | 86px（2行） | なし |
| 720px | 110px（3行に折り返す） | なし |
| 480px | 192px（さらに折り返す） | なし |

900px までは2行に収まり、狭くなると折り返して縦に伸びる。横スクロールは出ない。
**ただし 480px では村が見えなくなるほどヘッダが高い。** スマホでの利用は想定外のままにしている（要判断）。
