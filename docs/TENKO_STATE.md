# TENKO_STATE

最終更新: 2026-08-11

## 1. 目的

チャットと勤怠を繋いだオフィスアプリを実装する。
第一目的は転職。収益化は目的に含めない。

勤怠が面倒な理由は二重申告にある。
利用者はチャットで既に「10時に着きます」と報告しているのに、
勤怠システムはそれを知らないため、もう一度入力させている。

tenko では、チャットでの発言から勤怠の下書きを立てる。
確定は必ず人間が押す。機械は下書きまでしか作らない。

## 2. 画面

真上から見た平面の村。ドット絵で描く。バリアントAを採用（素朴な農村。木の壁と藁屋根、暖色寄り）。
BとCは判断の記録として残す。切り替えは src/sprites/index.ts の ACTIVE_VARIANT 1行。

**アバターはドラッグで自由に動かせる（Phase 4.8）。位置が状態を決める。**
初日に決めた「自由移動は実装しない / 状態が位置を決める」は、POの判断で入れ替わった。
3D は採用しない。実測の結果、同種のサービスが2次元で成立しているため。

動かせるのは自分のアバターだけ。移動の可否・座標の範囲・部屋の定員は、
すべて ws-server が判定する（画面側の判定だけでは、WSを直接叩けば通るため）。
移動のメッセージは利用者IDを持たず、届いた接続に紐づく人にしか適用しない。

村の要素と意味:

| 要素 | 意味 |
|---|---|
| 建物 | 部屋（rooms の1行 = 1棟）。kind で house / hall を決める。定員は rooms.capacity |
| 建物に入る | **その部屋で会議中になる。** 出れば入る前の状態に戻る |
| 建物の中の人物 | 描かない。乗せると部屋名・人数・中の人の名前が出る（案2。32ドットの建物に32ドットの人物は並ばないため）。**ただし自分だけは描く**（描かないと掴めず、建物から出られない） |
| 建物の窓が光る | 誰かがその部屋にいる |
| 建物の下の帯 | 何人入っているか。0人・途中・満員（赤）が区別できる |
| 建物の赤い枠 | 満員 |
| 建物の屋根の下の印 | 部屋の性格（部屋名から色と形を決める） |
| 建物の屋根の上の旗 | その部屋に未読の投稿がある |
| 自分の頭上の「勤怠 N」 | 確定していない勤怠の下書き。押すと確定・却下できる |
| 自分のアバターの金の枠 | 自分。50人いても探せるようにするため |
| 広場 | どの部屋にも入っていない状態 |
| 噴水 | 村のお知らせ。押すと開く。書けるのは admin のみ（Phase 4.9 で実装）。**噴水の上には立てない**（人が乗ると押せなくなるため） |
| 村の外 | 退勤 |

選べる状態は 在席 / 離席 / 休憩中 / 退勤 の4つ。
**「会議中」はメニューから外した。** 選ぶものではなく、建物に入った結果になったため。
これは Phase 3 の勤怠検出範囲（出社・退勤・休憩・遅刻）と対応する。

### 実装しないと決めたもの

- 家の中の内装: 部屋が増えるたびに内装が必要になり、会話までの手数も増える。建物をクリックしたらチャットが開く形を維持する
- レイアウトの編集機能: 各自が違う配置を持つと「あの家にいる」が通じなくなり、一覧性が失われる。編集画面自体が10〜15hの別機能になる
- ~~アバターのクリック移動・自由移動~~: **Phase 4.8 で撤回。POの判断でドラッグによる自由移動を入れた**
- 村のスクロール: 全員の位置が一目で分かることがこの画面の価値。村のサイズを固定し、配置密度で吸収する
- **近接音声（近づくと声が聞こえる）**: 採用しない。利用者が常に「聞かれているかもしれない」と意識する状態を作り、心理的安全性を損なうため（POの判断。Phase 4.8）

### 先送りしたもの

- 会話中の点滅: 頭上の点3つが実寸で見えにくい。1つ→2つ→3つと順に光らせて視認性を上げる。既存スプライトの分割のみで実現でき、新規の絵は不要。Phase 5 の仕上げで行う
- 広場での会話の表現: 建物の中でも広場に散らばっているのでもない「2人で軽く話している」状態がない。ベンチのような場所を置くのではなく、話している者同士を隣り合わせて配置し、関係を配置で示す。1対1の会話という概念がデータ設計に無いため、Phase 3 の後に設計する
- 部屋数が増えた場合の村の広さ: 現在640×416で画面に収まる。部屋が増えて収まらなくなった時点で再検討する

### 対応していないと明記するもの

- **深夜0時をまたぐ勤務**: 勤怠は発生時刻の日本時間の日付で記録するため、出社と退勤が別の日の記録になる。夜勤のある職場では正しくない。直すには「勤務日」の概念が要り、記録・月次・承認のすべてに影響するため未対応のままにする。README.md にも記載
- **認証**: 未実装。TENKO_TRUST_USER_PARAM=1 のままでは誰でも他人になりすませる。WebSocket サーバーには認証の口がない
- **自動離席の10分**: 仮説であり、実運用での裏付けはない

### Phase 4.5 で入れたもの

- 今日やること（吹き出し）: 1人1日1行。履歴なし。日付はサーバー（JST）が決める。村では常時3件まで表示し、残りは頭上の印
- 話しかけてOK / 後でならOK / 集中中: 在席とは独立した軸。WSのみ。ドット絵は増やさず足元の点で表す
- 自動離席: 操作が10分ない場合に away へ。idle への自動復帰はしない（人がいることを機械が主張しない）
- 勤怠異常の提示: 退勤なし / 長時間 / 出社なしの発言。読み取りのみで自動修正しない
- 勤怠の修正申請: 新しい記録として立て、元の記録を指す。承認は Phase 4 の仕組みを再利用

### Phase 4.8 で入れたもの

- **アバターの移動**: ドラッグのみ。座標は WS だけで持ち、DBには保存しない（失っても取り直せる）。
  送るのは100msに1回まで＋離した瞬間。配信は50msにまとめる
- **会議室の定員**: `rooms.capacity`（house=4 / hall=12。仮の値）。判定はサーバー側。満員なら入れない
- **呼びかけ**: 「いま話せますか」を伝えるところまで。通話は繋がない。
  相手が「いま話せます / あとで / いまは難しい」で返す。
  通話は**近接ではなく、この承認を経て始める**形にする（次のフェーズ）
- **「話しかけてよいか」との連動**: 集中中の相手には確認を挟む。呼びかけると「集中中と分かったうえで呼んだ」ことが相手に伝わる
- **トンマナの統一**: 村のパレット16色からCSS変数を作り、全画面で共通の部品を使う。角丸・影は使わない
## 3. 実測にもとづく設計方針

spike\ws-01（2026-08-11）の実測より確定。

- WebSocket の切断中の送信は、例外もエラーも出さずに消える
- サーバーログにも残らない
- 自動再接続は成立する
- バックグラウンド3分でも接続は維持される

したがって:

| データ | 経路 | 理由 |
|---|---|---|
| チャットの投稿 | HTTP + DB を正 → WS で通知 | 消えては困る |
| 勤怠の申請・承認・記録 | HTTP + DB のみ | 給与に直結する |
| 在席状態 | WS のみ | 失っても取り直せる |

## 4. 勤怠イベントの検出範囲

Phase 3 で実装する。最初は4種類に限定する。

- 出社
- 退勤
- 休憩
- 遅刻

有給・早退・直行直帰・残業申請は範囲外とする。
4種類でも「チャットが勤怠になる」という主張は成立するため。

検出は下書きを立てるまで。確定は人間が押す。

## 5. 技術構成

- Next.js（バージョンは固定値でピン留め）
- TypeScript
- PostgreSQL（Neon）
- WebSocket サーバーは ws-server ディレクトリに分離。素の Node.js + ws
- デプロイ先は Vercel（アプリ）と Railway（WSサーバー）を想定

Railway の無料枠は月1ドル分のクレジットであり、
常時起動するサーバーには足りない可能性が高い。
Phase 5 の直前に実測し、金額が判明した時点でPOが判断する。

## 6. フェーズ

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | WebSocket の実現性確認 | 完了 |
| 1 | チャット本体。投稿・履歴・未読 | 着手中 |
| 2 | 平面オフィスの描画と在席状態の同期 | 完了 |
| 3 | 発言から勤怠イベントを検出し下書きを立てる | 完了 |
| 4 | 承認と勤怠記録の確定、月次出力 | 完了 |
| 5 | 公開 | 未着手 |

## 7. 変更ログ

- 2026-08-11 初版作成。Phase 0 の実測結果を反映
- 2026-08-11 Phase 1 のスキーマを確定し Neon で実行。users / rooms / room_members / messages / read_states の5テーブル
- 2026-08-11 Phase 2 完了。バリアントAを採用し main にマージ。rooms に kind 列を追加し、建物の種別を並び順から独立させた
- 2026-08-12 Phase 4.5。今日やること・話しかけ可否・自動離席・勤怠異常の提示・修正申請を追加。DDLは追加のみ5文
- 2026-08-12 Phase 4.6。ブラウザで実機を見ながらUIを作り直し、全員分の一覧を追加。深夜0時をまたぐ勤務の未対応を明記
- 2026-08-12 Phase 4.7。`/` を村にし、チャットを `/rooms/[id]` へ移した。操作をヘッダからアバターへ移し、状態を足元のリングで示す形にした
- 2026-08-12 Phase 4.9。**建物に入れない不具合を直した**（判定が足元の1点にあり、下向きの許容が3ドットしかなかった）。村を1画面に収める縮尺にし、吹き出しを全員分出せるようにした（配置計算の結果が描画で使われていなかった）。噴水にお知らせを実装（DDL 3文）。在席の配信から名前を外した
- 2026-08-12 Phase 4.8。**設計方針を2点入れ替えた。**（1）自由移動を入れ、位置が状態を決める形にした（「会議中」は状態のメニューから外した）。（2）通話は近接音声ではなく「呼びかけ→承認」の形にすると決め、呼びかけまでを実装した。あわせて `rooms.capacity` を追加（DDL 3文）し、全画面のトンマナを村のパレットに揃えた
