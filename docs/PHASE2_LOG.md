# PHASE2_LOG

tenko Phase 2-B（夜間の長時間作業）の作業ログ。完了ごとに追記する。
POが朝に最初に読む文書。成功だけでなく、迷った点・却下した案・失敗も残す。

## 前提

- ブランチ: feature/phase2-village（feature/phase2-sprites から分岐）
- POは不在。判断を仰いで停止せず、選択肢を作るか、決めて記録して進む
- main への切り替え・マージ・push は行わない。DDL は実行しない

## 記録

### 起点
- ブランチ feature/phase2-village を作成。docs/PHASE2_LOG.md を新規作成した
- 前フェーズの評価（不合格6点）を作業の起点とする:
  1. 建物が引き出しに見える（屋根の横線が原因）
  2. 建物が小さすぎる（16x16）
  3. 草地が空虚
  4. 道が直線すぎる
  5. 人物に影がない
  6. 会話中の吹き出しが旗に見える

### 作業1・2 完了: スプライト3案（35枚ずつ）
- `scripts/gen-variants.js` で3案を生成。`src/sprites/variant-{a,b,c}.json`
- 鍵の一覧を生成器で一元管理し、3案で鍵が一致することを生成時に検査する作りにした
  - 実測: 各案 35枚 / パレット16色 / 「3案の鍵の集合: 完全に一致」
- 内訳: 人物16（4状態×4色）/ 建物4（house, house_lit, hall, hall_lit）/ 地面6 / 装飾8 / 噴水1

#### 迷った点と決定

| 迷った点 | 選んだもの | 却下したもの | 理由 |
|---|---|---|---|
| 装飾物の鍵の付け方 | `deco_1`..`deco_8` の通し番号 + JSON の `labels` に和名 | `tree` `well` など意味のある名前 | 世界観が違うと意味名が3案で揃わず、「鍵の集合が完全に一致」を満たせない。表示名は labels で持てば足りる |
| パレットの設計 | 全案で同じ意味のスロット（0透明/1地面明/…/15服C） | 案ごとに自由な割り当て | スロットの意味が揃っていれば、描画側も生成側も1つのコードで済む。16色の枠も守れる |
| 影の色 | 地面の暗い色（スロット2）を流用 | 影専用の色を追加 | 16色の枠を守るため。1〜2ドットなら専用色は不要だった |
| 4人目の服の色 | スロット12（紫）を新設し、水はスロット14（青）と共用 | 服4色＋水で17色 | 16色に収めるため。水と服Bが同じ青になるが、噴水と人物は大きさが違い混同しない |
| 建物の構図 | 斜め上から（上に屋根、下に壁・扉・窓） | 真上からの屋根のみ | 前回「引き出しに見える」原因は屋根の横線3本だった。屋根は面で塗り、線は棟と軒だけにした |
| hall の区別 | 屋根の色（スロット7）＋横幅＋窓3つ＋広い扉 | 新しい屋根色を追加 | 色を足さずに区別できた |

#### 失敗して直したこと
- `resting` の初版が `idle` とほぼ同じ見た目だった（8倍で見ても区別できず）。脚を右に投げ出した座り姿に描き直した
- 村の初版が空虚だった。装飾を24個だけ手で置いていたのが原因。`scripts/gen-map.js` を作り、道沿い・建物周り・外周に寄せて決定的に配置する方式に変えた（実測: 231個）
- 縦の道の右側だけ縁が直線のままだった。`path_edge_e` を左右反転して使う形にした（鍵を増やさずに済んだ）

### 作業3 完了: 比較用プレビュー
- `public/variants-preview.html`（実測 106030 バイト）。外部参照ゼロ、`file://` で開ける
- 3案を縦に並べ、各案に「村の全体像 4倍」「同 1倍」「人物4状態」「建物の通常/明るい版」「地面と装飾」「パレット」を載せた
- 冒頭に判断材料の表（世界観・色数・装飾数・CC評価）を置いた

#### 迷った点
- プレビューのデータ読み込み: **埋め込み**を選んだ。`file://` では JSON の fetch がブラウザに拒否されるため。代償として、JSON を直したら `node scripts\build-variants-preview.js` の再実行が必要

### 作業4 完了: 村の描画

- `src/sprites/index.ts` の `ACTIVE_VARIANT` を書き換えるだけで村全体が入れ替わる（現在は `"b"`）。画面上のボタンでも切り替えられるようにした（POが実機で見比べられるため）
- 地図は `src/village/map.json`。コードにベタ書きしていない
- `src/village/render.ts` に描画・配置・当たり判定をまとめ、画面（`src/app/village/page.tsx`）から呼ぶ
- 建物は `rooms` の行数だけ並ぶ。人物は在席状態で自動配置。自由移動は実装していない
- 建物を押すとその部屋のチャットへ移る（`/?room=<id>`）

#### 迷った点と決定

| 迷った点 | 選んだもの | 却下したもの | 理由 |
|---|---|---|---|
| 描画方式 | **Canvas** | DOM要素（div/img） | 40x26タイル + 装飾231 + 人物で1000要素を超える。状態が変わるたびに再描画すると DOM では重い。Canvas なら1枚に描き切れ、拡大も `image-rendering: pixelated` で足りる |
| チャット画面の部屋指定 | `/?room=<id>` のクエリを読む形に変更 | 村の中にチャットを埋め込む | Phase 1 の画面をそのまま使い回せる。**Phase 1 の設計方針（HTTP+DBを正）は変えていない**。変更は部屋番号の取得方法だけ |
| 建物と hall の割り当て | 部屋の並び順で5件に1件を hall にする | 部屋の種別をDBに持つ | 種別の列を足すのは DDL になる。今回は DDL を実行しない約束のため、表示側の規則で代用した。**要判断（後述）** |

### 作業5 完了: 在席状態の同期

- 状態は WebSocket のみ。DBには保存していない
- サーバー（`ws-server/index.js`）は接続ごとに在席を揮発メモリで持ち、変化があるたび**全体**を配る（差分ではない）
- 接続直後にも全体を送る。クライアントは再接続時に自分の状態を申告し直すため、一覧が再構成される
- 画面側が送れるのは `presence.*` だけ。それ以外は破棄する（WS経由で投稿を保存させないため）

#### 切断中の在席表示をどうするか（決定と理由）

**最後に受け取った一覧を残したまま、「表示は切断前の情報です」と添える。**

却下した案:
- 全員消す → 村が空になり「全員退勤した」ように見える。実際には自分の回線が切れただけで、他の人は在席している。誤った情報を出すより、古い情報だと明示する方が害が小さい
- 何も表示しない（画面をブロック） → 村の描画自体は在席がなくても成立する。見えるものを見せない理由がない

### 作業6 完了: 自己検証（すべて実測）

**ブラウザ操作はできないため、在席の確認は Node のクライアントで代替した。** 画面の見た目そのものは PO の目視待ち。

| # | 確認項目 | 結果 | 実測値 |
|---|---|---|---|
| 1 | 3案の鍵の集合が一致 | 成立 | 生成器の検査が「3案の鍵の集合: 完全に一致」。各案35枚 |
| 2 | 寸法が規定どおり | 成立 | 生成時に全スプライトを検査。人物/地面/装飾16x16、建物/噴水32x32、異常0件 |
| 3 | パレット16色以内 | 成立 | 3案とも16色 |
| 4 | file:// で単体表示 | 成立 | `variants-preview.html` に http/script src/link/fetch の出現ゼロ。106030バイト |
| 5 | tsc / eslint | 成立 | `tsc --noEmit` exit 0、`eslint src` exit 0（警告0件。初回はhookの依存3件の警告が出たので直した） |
| 6 | 3案とも例外なく描画 | 成立 | 実際の `render.ts` をコンパイルして Node から実行。a=341054 / b=341938 / c=351251 ドット、使用色15、補間切りtrue |
| 7 | 部屋を増やすと建物が増える | 成立 | DBに「会議室」をINSERT（DDLではない）。rooms 1件→2件、API も2件、`buildingRects` も1棟→2棟 |
| 8 | 一方の状態変更が他方に反映 | 成立 | タブAを talking@部屋1 にした 0.6秒後、タブBの一覧に `太郎=talking@部屋1` |
| 9 | WS停止中でも村が描ける | 成立 | ws停止中の `/village` が HTTP 200。在席が空でも描画ドット341392個、建物2棟 |
| 10 | 再起動で在席を取り直す | 成立 | 停止から13.33秒で再接続し、一覧が `太郎=talking@部屋1, 花子=idle` に復元。差分ではなく全体を配る設計が効いた |

#### 追加で行った破壊試験（指示外）
- 画面側のクライアントから `message.created` を偽装してWSに送り、他のクライアントに配信されないことを確認した（結果: 受け取っていない）。「WS経由では保存も配信もしない」という方針が実装で守られていることの確認

#### 検証中に気づいたこと
- PowerShell の `-replace` は既定で大文字小文字を無視する。`ROOM_ID` を `roomId` に置換したつもりが `room_id`（DBの列名）まで巻き込んで壊した。差分を見て発見し修正。既知のミス集に入れる価値がある
- 検証スクリプトを一時ディレクトリに置くと `ws` を解決できず失敗した。`scripts/` に置いて解決

### 追加作業: 装飾の並びの是正（実測して気づいた不具合）

3案で地図を共通にしたため、地図に書かれた `deco_1` が多い配置になっている。
ところが案ごとに `deco_1` の中身が違い、**C案が石灯籠だらけ、B案が街灯だらけの村**になっていた。
自分でPNGに描き出して見るまで気づかなかった。

対処: どの案でも `deco_1`/`deco_2` が「空白を埋める緑（木・低木）」になるよう並びを揃えた。
B案には街路樹と低木を新たに描き起こし、街灯は8番へ移した。

- A: 木 / 低木 / 柵 / 井戸 / 樽 / 畑 / 干し草 / 案山子
- B: 街路樹 / 低木 / ベンチ / 花壇 / 鉢植え / 石畳の模様 / 低い石塀 / 街灯
- C: 松 / 生垣 / 竹 / 石灯籠 / 井戸 / 暖簾 / 砂利だまり / 酒樽

---

## 朝の報告

### 1. 完了・未完了

| 作業 | 状態 |
|---|---|
| 1 スプライト仕様の共通化 | 完了。3案で鍵35枚が完全一致 |
| 2 バリアント3案 | 完了（A・B・C すべて） |
| 3 比較用プレビュー | 完了 |
| 4 村の描画コンポーネント | 完了 |
| 5 在席状態の同期 | 完了 |
| 6 自己検証 | 完了。10項目すべて実測。全項目 成立 |
| 7 朝の報告書 | この節 |
| 追加作業（4案目・時間帯の色調・地図の複数パターン） | **未着手**。優先度が低いため手を付けていない |

未完了として残したのは追加作業のみ。指示された作業1〜7はすべて完了している。

### 2. プレビューを開く手順

PowerShell に次の1行を貼れば開く。

```powershell
Start-Process "$env:USERPROFILE\OneDrive\デスクトップ\claude\tenko\public\variants-preview.html"
```

Next.js の起動は不要。ページの一番上に判断材料の表があり、その下に3案が縦に並ぶ。
各案に「村の全体像 4倍」「同 1倍（実際の見え方）」「人物4状態」「建物の通常/明るい版」「地面と装飾」「パレット」がある。

村の画面を実機で見る場合は、別のターミナルで次を実行してから `http://localhost:3000/village` を開く。

```powershell
cd "$env:USERPROFILE\OneDrive\デスクトップ\claude\tenko"; Start-Process node -ArgumentList "ws-server/index.js" -WindowStyle Hidden; npm run dev
```

2人分の動きを見るときは `http://localhost:3000/village?me=1` と `?me=2` を別のタブで開く。

### 3. 作業6の結果

10項目すべて成立。詳細は上の「作業6 完了」の表にある。要点のみ:

- 3案の鍵は完全一致（各35枚・16色）。寸法の異常0件
- `tsc --noEmit` / `eslint src` ともに exit 0・警告0
- 3案とも実コードで例外なく描画（34万ドット前後）
- 部屋を2件に増やすと建物も2棟になった（DBへのINSERTで確認。DDLは実行していない）
- 在席は0.6秒で他クライアントに反映。WS停止から13.33秒で再接続し、一覧が復元された
- WS停止中でも村は描ける（HTTP 200・在席が空でも34万ドット）

### 4. CCが推す案

**B（石造りの町）を推す。**

tenko は業務中に開いたままにする画面で、最も長く目に入る。A（農村）は村らしさでは最も強いが、
藁屋根の暖色が画面の広い面積を占め、長時間だと疲れる。C（和）は統一感があるが意匠の主張が強く、
利用者の好みが分かれる。B は彩度が最も低く、建物と人物のコントラストが安定していた。

ただし**「村らしさ」を最優先するなら A** を推す。柵・畑・案山子が入ると生活感が出て、
「オフィスを村に見立てる」という発想が最も伝わるのは A だった。この2案の選択は
tenko が何を優先するか（居心地か、業務ツールとしての無難さか）の判断であり、POの領分と考える。

### 5. POに判断してほしい点

CCが決めて進めたものも含む。後から覆せるよう全部挙げる。

1. **どの案を採用するか**（A / B / C）。切り替えは `src/sprites/index.ts` の `ACTIVE_VARIANT` 1行
2. **建物と hall の割り当て規則**。今は「部屋の並び順で5件に1件を hall」にしている。本来は部屋の種別で決めるべきだが、種別の列を足すのは DDL になるため見送った
3. **切断中の在席表示**。「最後の一覧を残し、古い情報だと明示する」を選んだ。消す方が正直だという判断もありうる
4. **チャット画面の部屋指定を `/?room=<id>` に変えた**。Phase 1 の画面に手を入れている（設計方針は変えていない）
5. **装飾の密度**（現在231個）。生成時の重みで増減できる。今の密度が濃すぎないか
6. **人物の吹き出しを点3つにした**。旗には見えなくなったが、控えめすぎないか
7. **村の広さ 40×26 タイル（640×416ドット）**。画面では2倍表示で 1280×832。部屋が20を超えると建物の枠が足りない
8. **在席の「退勤」の扱い**。今は village 画面のボタンで村から消えるだけ。勤怠記録とは無関係（Phase 4 で別に実装する前提）

### 6. 次にやるべきこと（提案）

1. 採用する案を決める（これが決まらないと以降の見た目が固まらない）
2. 会話中の判定を実データにつなぐ。今は在席の申告に部屋番号を載せているだけで、チャット画面を開いたことと連動していない
3. 部屋の種別（house / hall）をどこで持つかを決める。DDL が必要なら Phase 2 の最後にまとめて提示する
4. 村の広さと建物枠の上限（現在20）を、想定する部屋数に合わせて決める
5. Phase 2.5 の噴水（お知らせ）の中身を決める

### 7. git log

このログの最後にコミットハッシュを残す。

```
0a89bc3 fix(sprites): 装飾の並びを3案で揃え、朝の報告を追記
fae5213 feat(village): 村の描画と在席状態の同期を実装
930da2f feat(sprites): 村のバリアント3案と比較プレビューを追加
6b8bcaf feat(sprites): Phase 2 のドット絵18枚をJSONで定義しプレビューを追加
ef7836d Merge branch 'feature/phase1-chat'
a5cdd4e chore(scripts): Phase 1 の動作確認スクリプトを追加
20187d3 feat: Phase 1 の最小画面(投稿・履歴・接続状態・未送信の滞留)
477c23a feat(ws-server): 通知路に限定し、画面側からの送信を破棄する
6d3e5ff feat: 投稿の保存APIと差分取得APIを追加
fd73897 docs: TENKO_STATE.md に Phase 1 スキーマ確定を追記
50b47ad docs: TENKO_STATE.md を作成
920e95e chore: tenko の初期化(Next.js 16.3.0 + TypeScript + ws-server 分離)
```

（このコミット自身のハッシュは含まれない。最新は上記の1行目の1つ後になる）

### 終了時の状態
- ブランチ: feature/phase2-village。main への切り替え・マージ・push は一切していない
- 起動していた開発サーバーと ws-server は停止済み
- DBへの変更: rooms に「会議室」を1行 INSERT（作業6-7 の確認のため）。DDL は実行していない

---

## Phase 2-C: A案の採用と地図の修正

### 指摘3点の原因（座標と実測で確認）

指示では「3点はいずれも地図データに起因する」とされていたが、**修正前の地図を実測したところ、3点とも地図側には問題が無かった**。

```
地図: 40x26 タイル = 640x416 ドット / 中心 = ドット(320,208)
噴水: タイル(19,12) = ドット(304,192)〜(335,223) / 中心 ドット(320,208)  ← 村の中心と一致
道: 横 y=13 / 縦 x=20  ← 十字
建物の枠 20 = 左上5 右上5 左下5 右下5  ← 4区画に均等
```

原因は**プレビューHTML側**だった。4倍表示は 40×16×4 = 2560px 幅あり、ページの `max-width: 1400px` の枠に収まらず、`overflow-x: auto` の中で左端だけが見えていた。その範囲だけを見ると、
噴水が右端に、建物が左に偏り、縦道だけが通っているように見える。3点すべてがこの切り取りで説明できる。

対処:
- プレビューに「2倍・切れずに全体が入る」表示を追加し、これを最初に見る図にした
- 4倍表示には「この枠に収まらないので横スクロールが必要。左端だけを見ると噴水が右端にあるように見える」と注記を付けた
- `max-width` を 1500px に広げた

### 地図の修正（指示の趣旨に沿って行ったもの）

原因は別だったが、指示の趣旨（中心に集まり、密集し、4区画に分散する村）に沿う修正は行った。

1. **交差点寄りに建物の枠を6つ追加**（14,5）(24,5)(16,9)(23,9)(14,23)(24,23)。
   村の中心付近が空いていて「村は密集しているから村に見える」に反していたため。枠 20 → 26
2. **枠の並び順を4区画の順ぐりに変更**。これは本番の描画をPNGに出して初めて気づいた実質的な不具合で、
   建物は「部屋の並び順に枠を使う」ため、枠が左上→右上の順だと**部屋が少ないうちは建物が左上に固まる**。
   実際、部屋8件では左上5・右上3・下段0だった。並べ替え後は下表のとおり。

| 部屋数 | 左上 | 右上 | 左下 | 右下 |
|---|---|---|---|---|
| 4 | 1 | 1 | 1 | 1 |
| 8 | 2 | 2 | 2 | 2 |
| 12 | 3 | 3 | 3 | 3 |
| 20 | 5 | 5 | 5 | 5 |
| 26 | 7 | 7 | 6 | 6 |

3. 装飾は 231 → **257 個**（枠が増えて置ける面積が減るため、重みを 0.05 → 0.06 に上げた）

**スプライト（ドット絵）は1枚も変更していない。** 変更したのは配置データと生成スクリプトのみ。

### 目視確認（PNGに描き出して実施）

- 1倍の全体図: 噴水が中央、道が十字、建物が4区画、空虚な領域なし
- 本番の描画コード（コンパイルした `render.ts`）で描いた2倍の図: 部屋8件で4区画に2棟ずつ。会話中の部屋の窓が光っている

### 作業2: 実際の画面での確認

**ブラウザ操作はできないため、HTTP応答と、本番の描画コードによるPNG描画で代替した。**

| # | 項目 | 結果 |
|---|---|---|
| 1 | /village で村が描画されるか | 成立。HTTP 200 / 12822 バイト。本番コードで 640x416 を描画 |
| 2 | 配置がプレビューと一致するか | 成立。プレビューに埋め込まれた map.json と variant-a.json が、現在のファイルと完全一致することを確認（文字列比較 true） |
| 3 | 建物クリックでチャットが開くか | 成立。`hitBuilding` が部屋を返すことを確認済みで、遷移先 `/?room=1` `/?room=2` はいずれも HTTP 200 |
| 4 | 在席の変更が村に反映されるか | 成立。idle→広場(256,256) / talking@部屋1→建物直下(28,62) / resting→広場 / 退勤→描画0人。会話中の部屋 [1] が窓を光らせる対象になった |

### 作業3: 未使用スプライト

**A案の35枚すべてが使われている。未使用は0枚。**

装飾の地図上の出現数: 木63 / 低木72 / 柵36 / 井戸5 / 樽18 / 畑22 / 干し草27 / 案山子14。
井戸が5個と少ないのは、生成時の重みを 3 にしているため（村に井戸が何個もあると不自然と判断した）。
