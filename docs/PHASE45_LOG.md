# PHASE45_LOG

tenko Phase 4.5（仮想オフィスとしての完成度を上げる）の作業ログ。
POが朝に最初に読む文書。成功だけでなく、迷った点・却下した案・失敗も残す。

## 前提

- ブランチ: feature/phase45-office（main から分岐）
- POは不在。判断を仰いで停止せず、選択肢を作るか、決めて記録して進む
- main への切り替え・マージ・push は行わない
- 今回に限り tenko のDBへの DDL 実行が許可されている（追加のみ。DROP / TRUNCATE は行わない）

---

## 事前確定事項

### 1. 時刻とタイムゾーン

**実測してから決めた。** `scripts/probe-timezone.js` の出力:

```
DBサーバーの timezone 設定: {"TimeZone":"GMT"}
日本時間 8/12 00:30 -> JST基準の日付 = 2026-08-12 / DB既定の日付 = 2026-08-11
日本時間 8/13 08:59 -> JST基準の日付 = 2026-08-13 / DB既定の日付 = 2026-08-12
既存の attendance_records:
  id=1 work_date=2026-08-11 JST=2026-08-11 23:51 UTC=2026-08-11 14:51
```

- **DBサーバーの既定は GMT。** 何もしなければ日付は UTC で切られ、日本時間の 00:00〜09:00 が前日に倒れる
- 既存の実装（Phase 4）は `(event_at AT TIME ZONE 'Asia/Tokyo')::date` と明示しており、**JST 基準で正しく切れていた**（id=1 が JST 23:51 で work_date=8/11。UTC基準なら 8/11 14:51 でも同日だが、JST 00:30 のケースでは差が出る）

**決定: 業務上の1日の境界は JST（Asia/Tokyo）に統一する。** タイムゾーンは環境変数 `TENKO_TZ`（既定 `Asia/Tokyo`）とし、SQL には**パラメータとして束縛**する（文字列連結しない。S2）。

- 深夜0時をまたぐ勤務: 出社が 8/12 23:00、退勤が 8/13 02:00 の場合、**それぞれの発生時刻の JST 日付で別の日に記録される**。勤務の対を作って夜勤を1日に寄せることはしない
  - 理由: 対を作るには「どの出社とどの退勤が組か」の推測が要る。給与の根拠に推測を混ぜない（Phase 4 で休憩を回数に留めたのと同じ理由）
  - **この扱いは夜勤のある職場では正しくない。要判断（POへ）**

### 2. 「今日やること」のリセット判定

**決定: サーバー時刻（JST）で判定する。クライアントから渡された日付は一切使わない。**

セキュリティ上の理由: クライアント時刻を信用すると、端末の時計をずらすだけで任意の日付として振る舞える。
「今日やること」自体の実害は小さいが、**日付をクライアントから受ける形を1つ作ると、勤怠でも同じ形を使う誘惑が生じる**。
勤怠の日付をクライアントが決められる状態は改竄そのものであるため、最初から作らない。

実装: API は `note_date` をリクエストから受け取らない。サーバーで `(now() AT TIME ZONE $tz)::date` を使う。

### 3. 画面の目視ができないことへの対処

Chrome拡張が接続できないため、本番の描画コードで PNG に描き出して確認する（Phase 2 で確立した方法）。
**日本語グリフの描画は、自作の PNG 書き出しにフォント描画が無いため確認できない。**
吹き出しは「枠の位置・大きさ・折り返し・重なり」を実測し、文字の並びは箱で代替する。
グリフそのものは「未確認」として報告する。

---

## 記録

### 実装した5機能（すべて完了）

| 機能 | 状態 | 主なファイル |
|---|---|---|
| 1 今日やること（吹き出し） | 完了 | `src/lib/notes.ts` / `src/app/api/notes/route.ts` / `src/village/bubbles.ts` |
| 2 自動離席 | 完了 | `src/app/village/page.tsx` |
| 3 話しかけてOK/NG | 完了 | `src/village/render.ts` / `ws-server/index.js` |
| 4 勤怠異常の検出 | 完了 | `src/lib/anomaly.ts` / `src/app/api/attendance/anomalies/route.ts` |
| 5 勤怠の修正申請 | 完了 | `src/app/api/attendance/corrections/route.ts` / `src/app/attendance/page.tsx` |

### 実行したDDL（5文。すべて追加のみ）

`docs/PHASE45_SCHEMA.sql` に全文。ロールバックSQLは実行前に同ファイルへ記載済み。

1. `CREATE TABLE daily_notes`（今日やること。1人1日1行、履歴なし、本文80文字のCHECK付き）
2. `CREATE UNIQUE INDEX daily_notes_user_date_uq`
3. `ALTER TABLE attendance_records ADD COLUMN corrects_record_id`
4. `ALTER TABLE attendance_records ADD COLUMN correction_reason`
5. `CREATE INDEX attendance_records_corrects_idx`

適用は `scripts/apply-phase45-ddl.js --apply`。`--apply` なしでは対象を並べるだけで実行しない（J226）。
スクリプト内で `DROP` / `TRUNCATE` / `ALTER COLUMN` を含む文を検出したら実行前に落とすようにしてある。
既存のトリガー2つ・既存の列・制約には一切触れていない。

---

## 審査役ごとの指摘と対応

### 審査役A: セキュリティ（実際に送って確かめた）

**機能1「今日やること」への攻撃（`scripts/attack-notes.js`）**

| 送ったもの | 結果 |
|---|---|
| `<script>alert(1)</script>` | 200。文字列としてそのまま保存。表示側でエスケープ（下記の実測） |
| `<img src=x onerror="alert(1)">` | 同上 |
| 改行 `\n` `\r` の混入 | `"1行目 2行目 3行目"` に。改行は空白へ寄せた |
| タブ・ヌル文字 `\t` `\0` | `"a b c"` に |
| 双方向制御 RLO `‮` | 除去され `"abcgnp.exe"` |
| ゼロ幅スペース `​` | 除去され `"ab"` |
| 空文字 / 空白のみ | **400**「本文が空です」 |
| 80文字 / 81文字 / 10万文字 | 200 / **400** / **400**（長さはコードポイント数で判定） |
| `'); DROP TABLE daily_notes; --` | 200で保存。**テーブルは健在**（パラメータ化されている） |
| 数値・オブジェクト | **400**「本文は文字列で必要です」 |

**発見1: 絵文字が壊れた。** `👨‍👩‍👧‍👦` が `👨👩👧👦` に分解された。ZWJ(U+200D)を不可視文字として除いていたため。
→ ZWJ と異体字セレクタ(U+FE0F/FE0E)を残すよう修正。再実測で `設計レビュー 🙂🎌👨‍👩‍👧‍👦` がそのまま保存されることを確認。

**発見2: `user` を指定すれば他人の「今日やること」を書き換えられた。**
認証が未実装のため原理的に防げないが、**「どうせ認証がないから」で済ませない**（S6）ために、
利用者IDの解決を `src/lib/actor.ts` 一箇所に集約し、`TENKO_TRUST_USER_PARAM=0` でリクエストの user を
一切見ない動作に切り替えられるようにした。実測:

```
（TENKO_TRUST_USER_PARAM=0 で起動）
user=2 を指定 -> status=200 実際に書かれた先=1
user=3 を指定 -> status=200 実際に書かれた先=1
```

**HTMLとして解釈されないことの実測（`scripts/probe-escape.mjs`）**

```
入力: "<script>alert(1)</script>"
  出力: <div class="bubble">&lt;script&gt;alert(1)&lt;/script&gt;</div>
入力: "1 < 2 && 3 > 2"
  出力: <div class="bubble">1 &lt; 2 &amp;&amp; 3 &gt; 2</div>
比較（使っていない形）: dangerouslySetInnerHTML -> <div><script>alert(1)</script></div>
```

`src` 配下に `dangerouslySetInnerHTML` / `innerHTML` / `eval` / `new Function` は**1件もない**（機械的に確認）。
Canvas 側は `fillText` で描くため、そもそもHTMLとして解釈されない。

**機能4・5への攻撃（`scripts/attack-phase45.js`）**

```
異常の見える範囲:
  部下(member)    見える範囲=[2]           実際に出た利用者=[2]
  無関係(member)   見える範囲=[4]           実際に出た利用者=[]
  別上長(manager)  見える範囲=[5]           実際に出た利用者=[]
  担当上長(manager) 見える範囲=[2,3]         実際に出た利用者=[2]
  管理者(admin)    見える範囲=[1..6]        実際に出た利用者=[1,2]
  無関係な member が target=部下 を指定 -> 403「この利用者の勤怠を見る権限がありません」

修正申請:
  理由なし        -> 400  kindが不正 -> 400  日時が不正 -> 400  理由201文字 -> 400
  他人の記録を指す   -> 403「自分の記録ではありません」
  種別違いで直す    -> 400「元の記録は「leave」です。種別の違う記録では修正できません」
  正しい申請       -> 201（新しい記録として立ち、元の記録は変化なし）

修正申請の承認（Phase 4 の仕組みを通る）:
  本人(member) -> 403 / 無関係(member) -> 403 / 別上長(manager) -> 403 / 担当上長 -> 200
```

**発見3: 異常検出3種類のうち「出社の記録がないのに発言がある」が検出されなかった。**
`FULL OUTER JOIN` の結合条件が `days` 側の行を前提にしていたため、出社記録の無い日が落ちていた。
`keys`（両者の和集合）を作る形に直し、3種類とも出ることを実測（下記）。

**発見4: 修正申請の種別が元の記録と違っても通った。** 出社の記録を退勤で差し替えられ、
月次から元が消えて別種の記録が増える（勤務時間が実態と合わなくなる）。種別一致の検証を追加。

### 審査役B: 利用者の目（PNGで実測）

`scripts/render-bubbles.js`。本番の配置計算をそのまま使い、PNGに描いて数える。

**1周目（maxVisible=8）**

```
bub-8people : 人 8 / 吹き出し 8 / 重なり 5 / はみ出し 0
bub-20people: 人 20 / 吹き出し 8 / 重なり 5 / はみ出し 0
bub-long    : 人 8 / 吹き出し 8 / 重なり 7 / はみ出し 0
```

PNGを見ると、広場に並んだ人の吹き出しが横につながって**1本の帯**に見え、どれが誰のものか読めなかった。
村の一覧性（この画面の価値）が壊れている。

**対応**: 常時表示を3件に絞り、衝突回避（上へ段差）を入れ、表示しない人は頭上に小さな「メモあり」の印だけにした。

**2周目**

```
bub-8people : 人 8 / 吹き出し 3 / 畳んだ人数 5 / 重なり 0 / はみ出し 0
bub-20people: 人 20 / 吹き出し 3 / 畳んだ人数 17 / 重なり 0 / はみ出し 0
bub-long    : 人 8 / 吹き出し 3 / 重なり 0 / 最大2行・最大幅90px
bub-1person : 人 1 / 吹き出し 1 / 重なり 0
```

折り返しの実測:

```
長文  -> ["第3四半期の売上見込みの資料","をまとめて、部長と課長に共…"]
半角混じり -> ["ABC-1234 の設計レビューを 15",":00 から"]
絵文字 -> ["リリース🚀の準備と確認"]
```

**残っている指摘（未対応・朝の判断待ち）**: 吹き出しが建物の屋根にかぶることがある。3件までなので影響は限定的だが、
建物との衝突回避は入れていない。

### 審査役C: 設計の一貫性

- 既存方針との整合を確認: 投稿は HTTP+DB、WSは通知路、在席と話しかけ可否は WS のみ、機械は下書きまで。**いずれも崩していない**
- 発見4（種別一致）はこの審査で出た指摘
- `canApprove` の未使用インポートを削除（lint 警告）
- 月次の集計で、承認された修正に差し替えられた元の記録を除外する条件を追加（二重計上の防止）
- 戻り値だけでなく実体を確認（J224）: 異常検出の前後で `attendance_records` が1バイトも変わらないことを、
  全行のJSON比較で確認 -> `true`

### 審査役D: 運用する立場

| 観点 | 所見 |
|---|---|
| 毎日使って破綻しないか | 「今日やること」は1人1日1行で、100日使っても100行/人。日付が変われば自動で空になるため掃除が要らない |
| 戻し忘れ | 自動離席は away にするだけで idle には戻さない。**戻し忘れると「離席」のまま残る**。これは意図した設計（人がいることを機械が主張しない）だが、利用者は「戻したつもり」で離席のままになりうる。朝の判断事項に挙げる |
| 100日使うと | `attendance_records` は1日あたり数件×人数。異常検出は直近31日のみを見るため、増えても遅くならない |
| 管理者から見て | 異常検出は「打刻漏れを見つけて知らせる」を満たす。ただし**通知はしない**（画面を開いた人にだけ見える）。通知は未実装 |

---

## セキュリティ検証のまとめ（S1〜S7）

| 項目 | 結果 |
|---|---|
| S1 他人の入力が自分の画面に出る経路 | サーバー側で検証。制御文字除去・80文字・型。表示はReactのテキストとCanvasの fillText。`dangerouslySetInnerHTML` は不使用 |
| S2 SQLのパラメータ化 | 新規分はすべてパラメータ化。既存（Phase 1〜4）も確認し、文字列連結は**0件**。タイムゾーンもリテラルではなくパラメータ束縛 |
| S3 権限の検証はAPI側 | 異常検出・修正申請・承認のすべてでDBから role と manager_id を引き直して判定 |
| S4 他人のデータを取れないか | 実測。異常は見える範囲の積を取るため、target 指定でも越えられない（403） |
| S5 秘密情報 | `.env.local` はコミットしていない（`.gitignore` の `.env*`）。ログ・報告に接続文字列を出していない。エラー文にDB構造を含めていない |
| S6 認証未実装の前提 | `src/lib/actor.ts` に集約。`TENKO_TRUST_USER_PARAM=0` でなりすまし経路を塞げることを実測 |
| S7 新しい外部依存 | **追加なし**。npm パッケージは1つも増やしていない |

## 破壊試験（回帰）

```
WS経由の偽投稿        : messages 9 -> 9（弾かれている）
承認済みのUPDATE      : 例外「承認済みの勤怠記録は変更できません (id=1, approved_by=3)」
確定済みの下書きのUPDATE : 例外「確定または却下済みの下書きは変更できません (id=1, status=confirmed)」
他人の勤怠の承認（4通り）:
  本人(member) -> 403 記録=submitted
  無関係(member) -> 403 記録=submitted
  別上長(manager) -> 403 記録=submitted
  担当上長(manager) -> 200 記録=approved
```

## 機能4の実測（3種類とも検出）

```
閾値: {"longWorkHours":12,"days":31}
2026-08-10 検証 部下: [no_leave] 出社の記録はあるが、退勤の記録がありません
2026-08-09 検証 部下: [long_work] 出社から退勤まで 14.5 時間（閾値 12 時間）
2026-08-08 検証 部下: [message_without_arrive] 発言が 3 件ありますが、出社の記録がありません
今日(2026-08-12)の no_leave: 0 件（まだ勤務中の可能性があるため今日は除外）
```

## 機能5の実測（月次への反映）

```
修正前 : 2026-08-11,23:52,23:52,1,0,4,0,
申請後 : 2026-08-11,23:52,23:52,1,0,4,1,あり   ← 未承認として現れる
承認後 : 2026-08-11,23:52,19:15,1,0,4,0,       ← 退勤が差し替わる
元の記録: id=3 は status=approved のまま残る（履歴として消さない）
```

---

# 朝の報告書

## 1. 完了した機能と未完了の機能

**完了: 指定された5機能すべて。** 未完了の機能はない。
時間が余った分は、新しい機能を作らず審査役の周回に充てた（機能1は3周、機能4・5は2周）。

## 2〜5

「審査役ごとの指摘と対応」「セキュリティ検証のまとめ」「破壊試験」「実行したDDL」の各節に記載。

## 6. 各機能で迷った点と、選んだもの・却下したもの

| 機能 | 迷った点 | 選んだもの | 却下したもの | 理由 |
|---|---|---|---|---|
| 1 | 履歴を残すか | **残さない**（1人1日1行を上書き） | 履歴テーブル | 履歴を持つとタスク管理ツールの領域に入る。POの懸念（軸のぶれ）に沿う |
| 1 | 吹き出しの描き方 | **両方作った**（案A=Canvasに描く / 案B=HTMLを重ねる。画面のボタンで切替、既定は案B） | 片方だけ | 案Bは日本語の折り返しと可読性でブラウザに任せられる。案Aはドット絵の画風と揃う。POが実機で見比べられる形にした |
| 1 | 保存先 | **HTTP + DB** | WebSocketのみ | 他人に見せる文であり、消えると「今日やること」が空欄の人が出る。既存方針（失えないものはHTTP+DB）に従う |
| 1 | 常時表示の上限 | **3件**（残りは頭上の印） | 8件 | 8件では重なりが5件出て一覧性が壊れた（PNG実測） |
| 2 | 閾値 | **10分**（`NEXT_PUBLIC_TENKO_IDLE_MINUTES`） | 5分 / 30分 | 仮説。会議で画面を触らない時間を考えると5分は短すぎ、30分では「席にいない人が在席」の時間が長すぎる |
| 2 | 復帰時に自動で idle に戻すか | **戻さない** | 自動で戻す | 操作＝在席とは限らない（自動更新・別端末）。人がいることを機械が主張しない |
| 2 | 「操作」の判定 | pointerdown / keydown / wheel / touchstart と、タブが表示に戻ったとき | マウス移動 | マウス移動は席を離れても起きうる（振動・他アプリ）。意図のある操作に絞った |
| 3 | 組み合わせ爆発 | **人物のドット絵は増やさず、足元に色付きの点を重ねる** | 4状態×3段階=12枚のドット絵 | 12枚は作る量も維持も現実的でない。点なら状態と独立して足せる |
| 3 | 既定値 | **話しかけてOK** | 集中中 | 既定が「集中中」だと、設定しない人が全員話しかけにくくなる。村に行く理由が減る |
| 3 | 保存先 | **WSのみ** | DB | 在席と同じ性質（失っても再接続で取り直せる）。Phase 2 の方針に合わせた |
| 4 | 異常をテーブルに持つか | **持たない**（都度計算） | `anomalies` テーブル | 導出できるものを保存すると、元データと食い違う。書き込みが増えると「自動修正しない」保証も弱くなる |
| 4 | 今日の「退勤なし」 | **除外する** | 含める | まだ勤務中の可能性がある。毎日全員に異常が出ると誰も見なくなる |
| 5 | 修正の持ち方 | **新しい記録として立て、元を指す** | 元の記録を書き換える | 承認済みを書き換えない方針（Phase 4）と、トリガーによる凍結を守るため |
| 5 | 元の記録の扱い | **月次から除外するが行は残す** | 物理削除 | 監査の根拠が消える（J227） |
| 5 | 種別の変更 | **認めない**（種別一致を必須に） | 認める | 審査役Cの指摘。出社を退勤で差し替えると勤務時間が実態と合わなくなる |

## 7. 事前確定事項

- **業務上の1日の境界: JST（`TENKO_TZ`、既定 Asia/Tokyo）。** DBサーバーは GMT であることを実測で確認したうえで、SQLに明示してパラメータで束縛する
- **深夜0時をまたぐ勤務: 発生時刻のJST日付で別の日として記録する。** 出社と退勤の対を推測して1日に寄せることはしない
- **「今日やること」のリセット: サーバー時刻（JST）で判定。** クライアントから日付を受け取る口を作らない。実測で `note_date` を送っても無視されることを確認

## 8. 追加したnpmパッケージ

**なし。** 1つも追加していない。

## 9. 新機能の提案（実装していない）

| 機能名 | 入ると何が言えるようになるか | 想定工数 | なぜ今やらないか | 軸から外れる危険 |
|---|---|---|---|---|
| 異常の通知（朝の一覧をチャットに投げる） | 「打刻漏れが放置されない」 | 4〜6h | 現状は画面を開いた人にしか見えない。通知先の設計（誰に・いつ・どこへ）が未決 | 低。既存の噴水（お知らせ）と統合すべき |
| 「今日やること」の完了チェック | 「終わったことが分かる」 | 3〜4h | チェックが付くとタスク管理に一歩入る。POが懸念する領域 | **高**。Notion/タスク管理の方向 |
| 在席の履歴（誰がいつ席にいたか） | 「実働時間が勤怠と合っているか分かる」 | 8〜12h | 監視色が強い。在席をWSのみで持つ設計を崩す | 中。労務管理ツールの方向 |
| 部屋ごとの人数バッジ | 「どの部屋が賑わっているか一目で分かる」 | 2h | 村の情報量を増やす前に、既存の要素の可読性を確かめたい | 低 |
| 週次の勤怠サマリ | 「月末まで待たずに異常に気づける」 | 3h | 月次と重複する。月次の出力形式が固まってからでよい | 低 |
| 承認の一括操作 | 「承認者の手間が減る」 | 3h | 一括にすると根拠を見ずに押せてしまう。Phase 4 で「根拠を必ず見せる」と決めた設計と衝突する | 中 |

## 10. POに判断してほしい点

1. **吹き出しの描き方（案A: Canvas / 案B: HTML）。** 実機で見比べて決めてほしい。既定は案B
2. **常時表示の上限3件。** 少なすぎないか。残りは頭上の印のみになる
3. **自動離席の閾値10分。** 実運用の感覚と合うか
4. **自動離席から戻す操作を手動のみにした点。** 戻し忘れると離席のまま残る
5. **話しかけてOK/NGの既定値を「OK」にした点**
6. **深夜0時をまたぐ勤務を別の日として記録する点。** 夜勤のある職場では正しくない
7. **異常検出の閾値（長時間12時間・対象31日）**
8. **`TENKO_TRUST_USER_PARAM` の既定を 1（開発向け）にしている点。** 公開時は 0 が必要
9. **検証データがDBに溜まっている点。** 承認済みの記録はトリガーで消せないため、消すにはトリガーの一時無効化が要る

## 11. 現時点で答えを持たない事項

**要調査**
- 実運用での自動離席の閾値。10分は仮説であり、根拠となる実測はない
- 吹き出しの常時表示3件が、実際の人数（何人の職場か）に対して妥当かどうか
- 異常検出の閾値12時間が、実際の勤務実態に対して妥当かどうか
- 20人を超える村（30人・50人）での一覧性。20人までしか測っていない

**未検証**
- ブラウザ上での実際の見た目（Chrome拡張が接続できないため一度も見ていない）
- 日本語グリフの描画（自作PNG書き出しにフォント描画が無い）
- 案A（Canvasの吹き出し）の日本語の折り返しが、実際のフォント幅と合うか。配置計算は「全角1・半角0.5」の近似
- 複数の端末・複数のブラウザでの同時接続
- 自動離席が実際に10分後に発火するか（コードは書いたが、10分待つ実測はしていない。15秒間隔の判定ロジックのみ確認）
- WSサーバーを再起動したときの `talk` の復元（在席は再申告で復元されるが、talk 込みでの実測はしていない）

**仮説**
- 「村を見れば全員が今日何をしているか分かる」— 3件しか吹き出しが出ないため、実際には「3人分だけ分かる」。この形で主張が成立するかは未検証
- 「話しかけてOK/NGがSlackの解けていない問題を解く」— 使われなければ既定値のまま放置され、意味を失う可能性がある

## 12. PNGで確認していない見た目

- **日本語の文字そのもの**（グリフ）。吹き出しの中身は「文字が占める領域の帯」で代替して確認した
- **案B（HTMLの吹き出し）の見た目全般。** DOMのため、PNG書き出しの経路に乗らない。位置計算は共通だが、実際のフォント・行間・角丸は未確認
- **話しかけてOK/NGの点の見え方。** 描画コードは通っているが、点が実寸で判別できるかは未確認
- **勤怠の確認画面（`/attendance`）・承認画面（`/approvals`）の見た目。** HTTP 200 は確認済みだが、レイアウトは未確認
- **自動離席の案内文（「10分操作がないため自動で離席にしました」）の表示**

## 13. 認証を導入する際に変更が必要な箇所

**中心は1ファイル。** `src/lib/actor.ts` の `requestedUserId()` をセッションからの取得に差し替える。
呼び出し側は「解決済みの利用者ID」として扱っているため、原則そのままでよい。

ただし以下は個別に見直しが要る。

| 箇所 | 現状 | 認証導入時 |
|---|---|---|
| `src/lib/actor.ts` | リクエストの user を採用（`TENKO_TRUST_USER_PARAM` で無効化可） | セッションから取得。パラメータは一切見ない |
| `src/app/api/notes/route.ts` | `resolveUser(payload.user)` | 同上（呼び出しはそのまま） |
| `src/app/api/attendance/drafts/route.ts` / `[id]` | `body.user ?? CURRENT_USER_ID` を直接参照 | `requestedUserId` 経由に統一してから差し替え。**現状ここだけ共通化されていない** |
| `src/app/api/attendance/approvals/route.ts` / `[id]` | 同上 | 同上 |
| `src/app/api/attendance/monthly/route.ts` | `actor` パラメータ | セッションの利用者。`user`（対象）は残す（承認者が部下の分を見るため） |
| `src/app/village/page.tsx` / `approvals` / `attendance` | `?me=` で利用者を切り替え | 切り替えUIを外す |
| `ws-server/index.js` | クライアントが申告した `user` をそのまま信じる | **要設計。** WSにも認証が要る。現状は誰でも他人として在席を申告できる |

**最も危険なのは ws-server。** HTTPのAPIは `actor.ts` に集約したが、WSは接続時に何の検証もしていない。
認証を入れるときは、接続時にトークンを渡して検証する形が要る。今回は範囲外のため手を付けていない。

## 14. git log

```n5564493 feat(attendance): 勤怠異常の提示と修正申請を追加
42f28fd fix(bubbles): 再代入しない変数を const にする（lint）
4650572 feat(office): 今日やること・話しかけてOK/NG・自動離席を追加
a902409 Merge branch 'feature/phase4-approval'
4d5a360 chore(scripts): 承認の権限を組み合わせで確認するスクリプトを追加
b11b893 chore(scripts): Phase 3 の検証スクリプト2件を追加
cc0cd2b feat(approval): 上長の承認・差し戻しと月次CSV出力
918efe7 Merge branch 'feature/phase3-attendance'
c2974a2 fix(schema): BEFORE DELETE トリガーが削除を黙って抑止する問題の修正SQLを提示
5c33785 feat(attendance): 発言から勤怠の下書きを立てる（ルールベース）
1cc994c Merge branch 'feature/phase2-village'
f2d5205 feat(village): 建物の種別を rooms.kind から決める
```n

---

## 追加の周回（時間が余った分）

新しい機能は作らず、審査役の周回に充てた。

### 周回3: 利用者IDの解決を全APIで統一

報告13で「drafts / approvals / monthly だけ `actor.ts` を経由していない」と自分で挙げた点を、
朝を待たずに直した。5ファイル（drafts, drafts/[id], approvals, approvals/[id], monthly）を
`requestedUserId()` 経由に統一し、各ファイルの `CURRENT_USER_ID` の重複定義を削除した。

これにより、認証を入れるときに触る箇所は **`src/lib/actor.ts` の1関数と ws-server だけ**になった。

統一後にもう一度、全機能の攻撃と破壊試験を通した。結果は周回2と同一（すべて拒否/許可が意図どおり）。

```
異常の見える範囲   : 部下=[2] 無関係=[4] 別上長=[5] 担当上長=[2,3] 管理者=[1..6]
修正申請の検証     : 理由なし400 / kind不正400 / 日時不正400 / 201文字400 / 他人の記録403
修正申請の承認     : 本人403 / 無関係403 / 別上長403 / 担当上長200
破壊試験          : WS偽投稿は弾かれる / 承認済みUPDATEは例外 / 確定済み下書きUPDATEは例外
                    他人の勤怠の承認は4通りとも意図どおり
notesへの攻撃     : HTMLタグは文字列のまま / 絵文字は壊れない / 81文字と10万文字は400
                    SQLらしき文字列を入れてもテーブルは健在 / note_dateはサーバーが決める
異常検出の副作用   : 前後で attendance_records が同一（true）
```

### 残した宿題（朝の判断待ち）

- 吹き出しと建物の衝突回避（審査役B）
- ws-server の認証（範囲外。認証フェーズで設計）
- 検証データの整理（承認済みはトリガーで消せない）

## 新機能の提案（実装していない。判断はPO）

特則3に従い、書面での提案に留める。実装済みのものは捨てるのが惜しくなるため、提案の段階で止めている。

### 提案1: 異常の通知（噴水に出す）

- **入ると何が言えるようになるか**: 「打刻漏れが放置されない」。現状は画面を開いた人にしか見えないため、見ない人には届かない
- **想定工数**: 4〜6h
- **なぜ今やらないか**: 通知先の設計（誰に・いつ・どこへ）が未決。Phase 2.5 の噴水（お知らせ）と統合すべきで、噴水の仕様が決まる前に別経路を作ると重複する
- **軸から外れる危険**: 低。既存の要素の中で完結する

### 提案2: 部屋ごとの人数バッジ

- **入ると何が言えるようになるか**: 「どの部屋が賑わっているか一目で分かる」。今は建物の窓が光るだけで、2人なのか8人なのか分からない
- **想定工数**: 2h
- **なぜ今やらないか**: 村の情報量を増やす前に、今回入れた吹き出しと話しかけ可否の印が実機で読めるかを確かめたい
- **軸から外れる危険**: 低

### 提案3: 週次の勤怠サマリ

- **入ると何が言えるようになるか**: 「月末まで待たずに異常に気づける」
- **想定工数**: 3h
- **なぜ今やらないか**: 月次と処理が重複する。月次の出力形式がPOの検収を通ってからでよい
- **軸から外れる危険**: 低

### 提案4: 在席の履歴

- **入ると何が言えるようになるか**: 「実働時間が勤怠と合っているか分かる」
- **想定工数**: 8〜12h
- **なぜ今やらないか**: 在席をWSのみで持つ設計（Phase 2）を崩す。監視色が強く、村に行きたくなくなる方向に働く
- **軸から外れる危険**: 中。労務管理ツールの方向

### 提案5: 「今日やること」の完了チェック

- **入ると何が言えるようになるか**: 「終わったことが分かる」
- **想定工数**: 3〜4h
- **なぜ今やらないか**: チェックが付いた時点でタスク管理に一歩入る。POが最大の懸念として挙げている領域
- **軸から外れる危険**: **高**。Notion / タスク管理の方向

### 提案6: 承認の一括操作

- **入ると何が言えるようになるか**: 「承認者の手間が減る」
- **想定工数**: 3h
- **なぜ今やらないか**: 一括にすると根拠を見ずに押せる。Phase 4 で「根拠を必ず見せる」と決めた設計と正面から衝突する
- **軸から外れる危険**: 中。勤怠管理システムの方向
